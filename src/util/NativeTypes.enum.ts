export enum NativeTypes {
    Number,
    BigInt,
    Date,
    String,
    RegExp,
    Array,
    Int8Array,
    Object,
    Uint8Array,
    Uint8ClampedArray,
    Int16Array,
    Uint16Array,
    Int32Array,
    Uint32Array,
    Float32Array,
    Float64Array,
    BigInt64Array,
    BigUint64Array,
    Map,
    Set,
    WeakMap,
    WeakSet,
    Promise,
    Generator,
    GeneratorFunction,
    AsyncFunction,
    AsyncGenerator,
    AsyncGeneratorFunction,
}